meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: /requirements.txt
run:
  command: python x_work/manage.py runserver
  persistenceMount: /data
  containerPort: 8000